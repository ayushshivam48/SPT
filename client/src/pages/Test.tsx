import { useState, useEffect } from "react"; import api from "../api"; export default function Test() { const [serverStatus, setServerStatus] = useState("Checking..."); useEffect(() => { const testServer = async () => { try { const response = await api.get("/health"); setServerStatus(`✅ Server connected: ${JSON.stringify(response)}`); } catch (error) { setServerStatus(`❌ Server error: ${error instanceof Error ? error.message : "Unknown error"}`); } }; testServer(); }, []); return ( <div className="min-h-screen bg-gradient-to-br from-blue-50 to-indigo-100 p-8"> <div className="max-w-4xl mx-auto"> <h1 className="text-4xl font-bold text-center text-gray-800 mb-8">SPT Test Page</h1> <div className="bg-white rounded-xl shadow-lg p-6"> <h2 className="text-2xl font-semibold text-gray-700 mb-4">Tailwind CSS Test</h2> <div className="bg-blue-500 text-white p-4 rounded-lg shadow-lg">Tailwind is working!</div> </div> <div className="bg-white rounded-xl shadow-lg p-6 mt-4"> <h2 className="text-2xl font-semibold text-gray-700 mb-4">Server Status</h2> <p className="font-mono">{serverStatus}</p> </div> </div> </div> ); }
